# The worker drives builds and clones baselines, so it yields to the control
# services. Its own footprint is small; the compile itself is limited in
# docker.service.d/pareton-build-limits.conf.
[Service]
CPUAccounting=yes
CPUWeight=50
IOAccounting=yes
IOWeight=50
