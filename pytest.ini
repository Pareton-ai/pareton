[pytest]
markers =
    unit: fast tests, no GPU, no Docker, no network
    e2e: DB-backed end-to-end; requires PARETON_TEST_DATABASE_URL (Neon test branch)
    docker: requires local Docker daemon; skipped when unavailable
testpaths = tests
